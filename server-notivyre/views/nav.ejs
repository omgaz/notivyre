<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/"><%= siteTitle %></a>
      <div class="nav-collapse">
        <ul class="nav">
			<% for(page in navigation) { %>
			        <li class='<% if(navigation[page].path === currentPath || "/" + navigation[page].name === currentPath) { %>active<% } %>' data-pagename='<%= navigation[page].name %>'>
			            <a href='<%= navigation[page].path || "/" + navigation[page].name %>'>
			                <%= navigation[page].prettyName %>
			            </a>
						<div class="noti hide"><i>0</i></div>
			        </li>
			    <% } %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>